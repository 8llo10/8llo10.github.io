You do not need to place images here if you use /admin uploads.

Public image slots in the site:
1) Logo — upload from /admin → Brand & media
2) Main portrait — upload from /admin → Brand & media
3) About/event photo — upload from /admin → Brand & media
4) Project covers — upload inside each project in /admin → Projects
5) Custom section images — upload inside each section in /admin → Sections
